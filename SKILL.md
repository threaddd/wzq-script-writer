---
name: weizhengquan-script-writer
description: 微证券视频号信息流广告脚本筛选与撰写工作流，同时输出配套快讯文案。当用户提供新闻原文或脚本草稿，需要按"新闻分类 → 选题筛选 → 新闻切入 → 产品衔接（三类用户类型）→ 行动引导 → 快讯 → 输出前核查"框架进行脚本生成时使用。覆盖选题黑名单（11类）、12条合规红线、三类产品衔接模板，并内置输出前核查清单。
agent_created: true
version: 10.2
---

# 微证券视频号脚本撰写工作流

## 触发场景

- 用户提供新闻原文，要求生成视频号信息流脚本
- 用户提供已有脚本，要求合规审核和改稿
- 用户提及"微证券""筛选这条新闻""脚本撰写""按工作流"等关键词

## 工作流总览（六步）

```
输入（新闻原文 / 已有脚本）
    ↓
第一步：新闻分类（低门槛 / 高门槛）
    ↓
第二步：选题筛选 ← 关键卡点
   ├ A. 黑名单（11 类，命中任一即筛除）
   └ B. 三大入选原则（紧迫性/重要性/接近性，至少命中一项）
    ↓ 通过则继续，否则止步
第三步：脚本撰写
    ├ a. 新闻切入（最多 2 个数字，时间模糊化）
    ├ b. 产品衔接（三类用户类型，逻辑自然生长）
    └ c. 行动引导（4 个 CTA 轮换，连续两条不重复）
    ↓
第四步：快讯撰写（热点事实 + 过渡词 + 轻 CTA，≤25 字）
    ↓
第五步：输出前核查清单（12 项逐条过）
    ↓
第六步：按输出格式落盘
输出：完整脚本 + 快讯文案 + 日期 + 来源
```

## 关键约束

1. **第二步未通过即停止后续输出**：命中黑名单或三大原则均不命中时，仅输出筛选结果和建议，不再生成脚本
2. **数据必须模糊化 + 来源化**：所有具体数据最多保留 2 个，统一用「据相关数据」，不出现机构名
3. **时间必须模糊化**：禁用「昨天/昨晚/今天/明天/下周/X月X日」，统一改用「最近/近期」
4. **产品衔接不能无逻辑并列**：产品出场前必须有承接（观众已产生疑问或需求），三类模板连续不重复
5. **落点（行动引导）不与主体冗余**：主体段说过的产品卖点，CTA 不再重复；CTA 句式连续两条不重复
6. **整体语言通俗化**：目标用户是投资新手，避免专业术语堆砌，不用破折号（——）

## 详细执行规则

完整规则见 `instruction.md`。需要查阅细节时按需加载：

- 黑名单 11 类详情 → `references/blacklist.md`
- 专业词通俗化对照 → `references/glossary.md`
- 合规红线 12 条 → `references/compliance.md`
- 三类用户衔接 + 行动引导 CTA 句式库 → `references/landing.md`

## 输出格式

严格按照 `instruction.md` 中【六、输出格式】部分执行：

```
【完整脚本】（包含主体和行动引导，不需要分别标注，空行分隔即可）

...主体内容...

...行动引导...

【快讯文案】「...」（xx字）
【日期】时间截止至x月x日（无法提取则标注：⚠️ 请人工补充）
【来源】xxx（无法提取则标注：⚠️ 请人工补充）
```

> 注：新闻分类、选题筛选、衔接类型选择均为**内部执行步骤**，不作为输出字段呈现。选题不通过时，停止输出脚本，仅说明不通过原因和修改建议。

## 输入示例

- 新闻原文输入：`examples/01-news-input.md`
- 已有脚本审核：`examples/02-script-review.md`
- 命中黑名单的止步示例：`examples/03-blocked.md`
- URL 输入：`examples/04-url-input.md`

## 版本历史

- v10.2（当前）：新增输出前核查清单（12 项）；日期禁用词补「今天」；合并 v10 系列重构成果
- v10.0：删除前贴环节，只保留主体 + 行动引导；新增三类用户衔接模板（不了解/消息过载/跃跃欲试）；快讯轻量化（≤20字）
- v9.1：新增数据引用规范，禁用估算/推算数据
- v9.0：黑名单扩展到 11 类，新增涉外敏感/大宗负面/跟随性炒作/医疗健康
- v8.0：身份触发句六种变体表，落点恢复 ABC 三型分支

完整变更日志见 `CHANGELOG.md`。
