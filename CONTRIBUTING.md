# 贡献指南

感谢你对本 Skill 的关注！欢迎通过 Issue 或 PR 帮助完善这套工作流。

## 贡献方式

### 1. 提 Issue

适合以下情况：

- 发现合规边界争议（某句话是否违规？）
- 遇到指令未覆盖的新情况
- 提出新的黑名单类型
- 报告生成质量问题

**Issue 模板**：

```markdown
## 类型
[ ] 合规边界争议
[ ] 指令规则缺失
[ ] 黑名单建议补充
[ ] 输出质量问题
[ ] 其他

## 输入示例
（贴出触发问题的新闻或脚本）

## 实际输出
（贴出 AI 生成的不符合预期的内容）

## 期望输出
（说明应该怎么处理）

## 关联指令位置
（指出 instruction.md 中相关章节）
```

### 2. 提 PR

适合以下情况：

- 扩充 examples（新案例）
- 补充 glossary 对照词
- 改进 references 中的句式库
- 完善文档表述

**PR 流程**：

1. Fork 本仓库
2. 创建特性分支：`git checkout -b feature/your-improvement`
3. 提交变更，commit message 用中文清晰说明
4. **同步更新版本号和 CHANGELOG**：
   - 小修小补：不需要升版本
   - 新增规则 / 类型：升 minor 版本（v9.1 → v9.2）
   - 重大结构变更：升 major 版本（v9.x → v10.0）
5. 提交 PR，描述变更原因和影响范围

## 内容规范

### Markdown 格式

- 标题统一使用 `#`、`##`、`###` 层级
- 列表用 `-`，不用 `*`
- 代码块用三反引号 + 语言标识
- 表格保持对齐，每列 `|` 前后留一个空格

### 用词

- "用户" 而不是"客户"
- "投资者" 而不是"散户"（除非特指）
- "微证券" 而不是"产品"

### 案例

新增违规案例时，必须包含：

- 原文（违规版）
- 命中条款编号
- 修改方向 / 替换文本

## 不接受的修改

❌ 添加任何具体个股推荐示例
❌ 添加任何承诺收益的案例（即使是"反面教材"也要打码）
❌ 涉及政治敏感话题的案例
❌ 涉及竞品贬低的内容

## 联系方式

提 Issue 或 PR 即可，所有讨论都在 GitHub 上进行。
